{
  "watch": ["src"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "dist", "node_modules", "uploads"],
  "exec": "ts-node --transpile-only server.ts",
  "env": {
    "NODE_ENV": "development",
    "NODE_OPTIONS": "--no-deprecation"
  }
}
